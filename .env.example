# Optional: override frontend API base URL when not using the Vite proxy.
VITE_API_BASE_URL="http://127.0.0.1:3001"

# Optional: override the frontend dev proxy target.
VITE_API_PROXY_TARGET="http://127.0.0.1:3001"
